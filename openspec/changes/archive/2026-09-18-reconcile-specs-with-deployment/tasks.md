## 1. Reconcile

- [x] 1.1 Verify live: only `pea-image-1` (GPU 8, `:5100`) runs; GPU 7 hosts `pea-speech-*`; `heartcode-image` has one deployment and `min_replicas: 1` (checked 2026-09-18)
- [x] 1.2 Replace (REMOVED + ADDED) the `gpu-rebalance` image requirement to match
