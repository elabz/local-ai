# Vision-embed service — Python dependencies.
# torch + torchvision are installed separately in the Dockerfile (CUDA cu121
# wheels, sm_61). The nomic v1.5 pair loads via transformers trust_remote_code.

# Web framework (mirrors multimodal-embed)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.18   # CVE-2024-24762, CVE-2024-53981
httpx>=0.26.0              # http(s) image-url fetching

# Metrics and monitoring
prometheus-client==0.19.0
pynvml==11.5.0

# Configuration
pydantic>=2.5.3
pydantic-settings>=2.1.0

# Utilities
orjson>=3.9.15             # CVE-2024-27454
pillow>=10.2.0             # image decoding

# --- nomic embedding model stack ---
# nomic-embed-vision-v1.5 + nomic-embed-text-v1.5 are trust_remote_code models;
# the text tower (nomic-bert) needs einops. Keep transformers < 5.0.
transformers>=4.40.0,<5.0
einops>=0.7.0
safetensors>=0.4.2
