# gpu-server unit-test dependencies (CI `unit-tests` job and local runs).
#
# Pinned so a clean install resolves the same versions on every run. The web
# stack mirrors what the deployed chat image actually runs (`pip freeze` in
# pea-gpu-1, 2026-09-18) — bump these together with gpu-server/requirements.txt.
#
#   python -m venv venv && venv/bin/pip install -r gpu-server/requirements-test.txt
#   venv/bin/python -m pytest gpu-server/tests --ignore=gpu-server/tests/integration
#
# torch is deliberately absent: it belongs to the GPU images, and the few
# tensor tests that need it skip via pytest.importorskip.

fastapi==0.125.0
starlette==0.50.0
uvicorn==0.40.0
pydantic==2.5.3
pydantic-core==2.14.6
pydantic-settings==2.1.0
httpx==0.28.1
sse-starlette==1.8.2
prometheus-client==0.19.0
orjson==3.11.7
pyyaml==6.0.3
numpy==2.3.5
pytest==8.4.2
