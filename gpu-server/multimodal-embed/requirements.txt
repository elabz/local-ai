# Multimodal Embedding Server - Python dependencies
# torch is installed separately in the Dockerfile (CUDA cu121 wheel, sm_61).

# Web framework (mirrors gpu-server/requirements.txt)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.18  # CVE-2024-24762, CVE-2024-53981
httpx>=0.26.0             # http(s) image-url fetching

# Metrics and monitoring
prometheus-client==0.19.0
pynvml==11.5.0

# Configuration
pydantic>=2.5.3
pydantic-settings>=2.1.0

# Utilities
orjson>=3.9.15            # CVE-2024-27454
pillow>=10.2.0            # image decoding

# --- Multimodal embedding model stack ---
# nomic-embed-multimodal-3b is a PEFT/LoRA adapter on Qwen2.5-VL-3B-Instruct.
# BiQwen2_5 support landed in colpali-engine 0.3.9; 0.3.14 bumped runtime to
# transformers>=5.0 which Nomic advises against (use transformers<5.0), so pin
# the last pre-transformers-5 release line. Re-pin exact versions after the
# on-hardware build succeeds (openspec tasks 4.3/4.4).
colpali-engine==0.3.13
transformers>=4.49.1,<5.0
accelerate>=0.26.0
peft>=0.13.0
qwen-vl-utils>=0.0.8
