services:
  vision-embed-1:
    image: local-ai-vision-embed:gpu-health-preflight
    environment:
      GPU_HEALTH_ENABLED: "1"
  vision-embed-2:
    image: local-ai-vision-embed:gpu-health-preflight
    environment:
      GPU_HEALTH_ENABLED: "1"
  dino-embed-1:
    image: local-ai-dino-embed:gpu-health-preflight
    environment:
      GPU_HEALTH_ENABLED: "1"
  dino-embed-2:
    image: local-ai-dino-embed:gpu-health-preflight
    environment:
      GPU_HEALTH_ENABLED: "1"
  gpu-server-1:
    environment:
      GPU_HEALTH_ENABLED: "1"
  gpu-server-2:
    environment:
      GPU_HEALTH_ENABLED: "1"
  gpu-server-3:
    environment:
      GPU_HEALTH_ENABLED: "1"
  gpu-server-4:
    environment:
      GPU_HEALTH_ENABLED: "1"
  gpu-server-5:
    environment:
      GPU_HEALTH_ENABLED: "1"
  gpu-server-6:
    environment:
      GPU_HEALTH_ENABLED: "1"
