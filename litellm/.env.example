# Local AI - LiteLLM Proxy Environment Variables
# Copy this file to .env and fill in the values:
#   cp .env.example .env

# =============================================================================
# REQUIRED: Master key for LiteLLM admin API
# Generate: python3 -c "import secrets; print('sk-' + secrets.token_urlsafe(24))"
# =============================================================================
LITELLM_MASTER_KEY=sk-CHANGE-ME-generate-a-new-key

# =============================================================================
# REQUIRED: PostgreSQL password for API key storage
# =============================================================================
LITELLM_DB_PASSWORD=change-me-use-a-strong-password

# =============================================================================
# LiteLLM settings
# =============================================================================
LITELLM_LOG=INFO

# =============================================================================
# Optional: Langfuse for LLM observability
# Get these from Langfuse UI > Settings > API Keys
# =============================================================================
# LANGFUSE_PUBLIC_KEY=pk-lf-xxx
# LANGFUSE_SECRET_KEY=sk-lf-xxx
# LANGFUSE_HOST=http://langfuse-web:3000
