# Grafana Dashboard Provisioning

apiVersion: 1

providers:
  - name: 'HeartCode Dashboards'
    orgId: 1
    folder: 'HeartCode'
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
